{
  "devDependencies": {
    "cypress": "^13.15.2",
    "cypress-file-upload": "^5.0.8",
    "mochawesome": "^7.1.3",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0"
  },
  "scripts": {
    "merge-reports": "npx mochawesome-merge cypress/results/*.json > cypress/results/combined.json && npx mochawesome-report-generator cypress/results/combined.json -o cypress/results"
  }
}
